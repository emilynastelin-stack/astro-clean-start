---
---
<style>
footer {
  background-color: #800000; /* maroon */
  color: white;
  padding: 1rem 2rem;
  display: flex;
  align-items: center;
  justify-content: center; /* center links */
  font-family: sans-serif;
  font-size: 1rem;
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  flex-wrap: wrap;
  z-index: 100;
}

/* Left two-line text */
.left {
  position: absolute;
  left: 15px;
  display: flex;
  align-items: center;
}

.two-lines {
  display: flex;
  flex-direction: column;
  justify-content: center;
  line-height: 1.2;
}

/* Center navigation links */
.center {
  display: flex;
  gap: 2rem;
  justify-content: center;
  flex: 1;
}

.center a {
  color: white;
  text-decoration: none;
  padding: 0.5rem 1rem;
  border-radius: 6px;
  background: rgba(255, 255, 255, 0.1);
  transition: all 0.3s ease;
  font-weight: 500;
  border: 1px solid rgba(255, 255, 255, 0.2);
}

.center a:hover {
  background: rgba(255, 255, 255, 0.2);
  border-color: rgba(255, 255, 255, 0.4);
  transform: translateY(-1px);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
}

/* Right copyright / @ */
.right {
  position: absolute;
  right: 15px;
}

/* Hide footer on mobile by default */
@media (max-width: 768px) {
  footer {
    display: none;
  }
}

/* Show footer on mobile only on results page */
@media (max-width: 768px) {
  body.results-page footer {
    display: flex;
    flex-direction: column;
    text-align: center;
    height: 50px; /* smaller height than navbar */
    font-size: 1rem; /* slightly smaller text */
    justify-content: center;
    padding: 0.5rem 1rem; /* reduced padding */
  }

  /* Hide desktop content on mobile results page */
  body.results-page .left, 
  body.results-page .center, 
  body.results-page .right {
    display: none;
  }
  
  /* Show mobile title on mobile results page */
  body.results-page .mobile-title {
    display: block;
  }
  
  .mobile-title {
    display: none;
    color: white;
    text-align: center;
  }
}
</style>

<footer>
  <!-- Left two-line text: English + Russian -->
  <div class="left">
    <div class="two-lines">
      <div>Archive of Soviet Trumpet Music</div>
      <div>Архив советской трубной музыки</div>
    </div>
  </div>

  <!-- Center navigation -->
  <div class="center">
    <a href="/" data-translate="nav.home">Home</a>
    <a href="/about" data-translate="nav.about">About</a>
    <a href="/sources" data-translate="nav.sources">Sources</a>
    <a href="/submit" data-translate="nav.submit">Submit</a>
  </div>

  <!-- Right copyright / @ -->
  <div class="right">&copy; 2025 Archive of Soviet Trumpet Music</div>
  
  <!-- Mobile title (only shows on mobile results page) -->
  <div class="mobile-title" data-translate="nav.brand.primary">Archive of Soviet Trumpet Music</div>
</footer>
